# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
!.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# backup scripts (DB URL が含まれるためgit管理外)
/scripts/backup.ps1

# DB バックアップダンプ（本番データ含むため git 管理外）
/backups

# age バックアップ秘密鍵（絶対にコミットしない。失うと全 dump が復号不能）
bract-backup-key.txt
*.age
*.sql.gz.age

# Playwright E2E
/playwright-report
/test-results
/tests/e2e/.auth

# next-pwa が生成するService Workerファイル（ビルド時に自動生成）
/public/sw.js
/public/swe-worker-*.js
/public/workbox-*.js

# ビルド検証ログ（手元の一時ファイル）
/build-*.log

# Claude Code の worktree コピー残骸（複製時に紛れ込む。git 管理外）
/.claude/worktrees/
/docs/legal/_out/
